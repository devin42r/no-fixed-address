{/*import React, { Component } from 'react';
import { StyleSheet, AppRegistry, Text, View, Image, Alert, Button } from 'react-native';

export default class App extends React.Component {
  render() {
      let pic = {
        uri: 'https://upload.wikimedia.org/wikipedia/commons/d/de/Bananavarieties.jpg'
      }
    return (
      <View style={styles.container}>
          <View style={{padding: 20}}>
            <Image source={pic} style={{width: 200, height: 200, borderWidth: 2, borderRadius: 100}}/>
          </View>
          
          <Text style={{color: 'white', fontWeight: 'bold', fontSize: 30}}>firstName Lastname</Text> 
          
          <View style={{alignItems: 'flex-start', padding: 10}}>
            <Text>
              Tell us all about yourself
            </Text>
          </View>
         
          <View style={styles.button}>
            <Button
              onPress={() => { Alert.alert('button pressed')}}
              title="About"
            />
          </View>
          <View style={styles.button}>
            <Button
              onPress={() => { Alert.alert('button pressed')}}
              title="Items"
            />
          </View>
          <View style={styles.button}>
            <Button
              onPress={() => { Alert.alert('button pressed')}}
              title="Donate"
            />
          </View>
      </View>
    );
  }
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
        backgroundColor: 'deepskyblue',
    alignItems: 'center',
        justifyContent: 'flex-start',
        flexDirection: 'column',
  },
        button: {
        marginBottom: 30,
        width: 260,
      }
});*/}